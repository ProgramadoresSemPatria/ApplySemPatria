# DeFi.jobs

- **URL:** `https://www.defi.jobs/` then `/jobs/{slug}`
- **Method:** Static HTML scrape (Webflow)
- **Fields:** Job links from homepage; detail from job page `<title>`
- **Note:** Smaller volume; keyword filter on slug/title
- **Apply channel:** `external_url`
