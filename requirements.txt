# Core CLI (discover, table, profile, ResumeChameleon)
python-docx>=1.1.0
pymupdf>=1.24.0

# Optional groups installed by: jobsearch install [--gmail] [--browser]

# Gmail API send
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# LinkedIn DM / browser flows (Patchright)
patchright>=1.62.0
